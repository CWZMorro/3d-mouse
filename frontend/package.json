{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "vite": "vite",
    "tunnel": "node tunnel.js",
    "dev": "concurrently \"pnpm vite\" \"pnpm tunnel\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "localtunnel": "^2.0.2",
    "socket.io-client": "^4.8.3",
    "untun": "^0.1.3"
  }
}
